# Validation and Evidence

**Status:** Proposed validation catalogue  
**Owner:** Evidence definitions, acceptance mapping and completion claims

## Evidence rules
- `Implemented` means relevant source/config exists; it does not prove build/runtime.
- `Ran` names exact command/environment.
- `Passed` applies only to that check.
- `Verified` means evidence was inspected against its declared contract.
- `Skipped` and `Unproven` are not `Failed`, but cannot support the missing claim.
- Build, unit, integration, browser, accessibility, security, migration, restore and production evidence are distinct.
- Put each important assertion at the lowest-cost level that still contains its failure mechanism.
- Do not duplicate the same combinatorial assertions at every layer.

Prior `VAL-001`–`VAL-020` are legacy-reserved because their original definitions were not supplied. This revision uses new IDs.

## Validation catalogue
| ID | Check | Evidence boundary |
|---|---|---|
| `VAL-021` | Governance structure audit | canonical paths, links, duplicate owners, ID uniqueness, size limits |
| `VAL-022` | Repository baseline inspection | real root, branch/remotes/working tree/ignore/history facts only |
| `VAL-023` | Clean application build/static checks | selected toolchain install/build/type/static/lint as configured; not runtime |
| `VAL-024` | Authentication/authorisation | account lifecycle, sessions, ownership, negative cross-user/pro-role access |
| `VAL-025` | Assessment/version/safety | save/resume, template history, safety flags, conservative stop/restriction |
| `VAL-026` | Exercise/content versioning | schema/invariants, publication states, historical exact-version retention |
| `VAL-027` | Routine generation constraints | property/fixture tests for equipment, goals, restrictions, structure, substitutions and fail-closed cases |
| `VAL-028` | Scheduling/progression | recurrence/timezone/exceptions/idempotency, explanation/reversal, conservative reduction |
| `VAL-029` | Session/print parity | active-session persistence and identical snapshot/dosage in interactive/print/PDF |
| `VAL-030` | Reporting/provenance | derived metric calculation, labels and source-record traceability |
| `VAL-031` | Meal/allergen integrity | recipe/meal versions, exclusions/allergens, shopping-list consistency |
| `VAL-032` | Cross-device concurrency | lost-update conflicts, reconnect/retry/idempotency, data-preserving resolution |
| `VAL-033` | Privacy/logging/data lifecycle | collection surface, log/analytics redaction, export/correction/deletion/retention behaviour |
| `VAL-034` | Accessibility/UX state | automated scan plus keyboard/focus/errors/zoom/reflow/assistive-tech checks for mapped flows |
| `VAL-035` | Migration/recovery | forward migration, failure rollback/restore, historical semantics, backup restore where applicable |
| `VAL-036` | Security boundary review | untrusted inputs, authz, secrets, TLS/config, destructive actions, dependencies as applicable |
| `VAL-037` | Performance/support matrix | only declared latency/load/browser/device/resource targets; representative runtime evidence |
| `VAL-038` | Entitlement/professional isolation | free capability gates, expiry, content provenance, no initial user-record access by professional roles |
| `VAL-039` | Release smoke/rollback | production-like deploy/start/core flow/rollback or recovery for declared release target |

## Recorded repository evidence

### `BR-20260817-02`
- **Mode/scope:** Repository execution; `PH-00`, `VAL-021`, `VAL-022`.
- **Governance evidence:** all manifest-governed files passed byte and SHA-256 verification after the stale `PROJECT_SETTINGS.md`/`REVISION_AUDIT.md` manifest entries were repaired; the staged initial-commit scope was explicitly reviewed.
- **Local evidence:** commit `0f3db2b5abda7f4fea6315baa01218dade562caa` exists on clean local `main`, with `main` tracking `origin/main` at the same commit.
- **Remote evidence:** independent GitHub inspection verified `refs/heads/main` at `0f3db2b5abda7f4fea6315baa01218dade562caa` with commit message `docs: establish MeExercise governance v0.2.0`.
- **Passed / Verified:** governance structure/integrity, repository baseline, commit and remote publication for PH-00.
- **Unproven:** application framework/toolchain, dependencies, build, tests, runtime, deployment and all PH-01+ behaviour.

### `BR-20260817-03`
- **Mode/scope:** Governance path canonicalization; PH-00 maintenance; `VAL-021`, `VAL-022`.
- **Starting evidence:** clean local `main` and `origin/main` at `4bf02cf47ebc31bde796938950b76a265c7603a8` before mutation.
- **Change:** `PROJECT_SETTINGS.md` names the tracked repository root and `project_docs/` as canonical governance locations; repository policy designates detached/versioned source-pack copies as non-canonical after integration.
- **Validation contract:** exact changed-file scope, manifest byte/SHA-256 verification, project-settings size limit, staged-content check, PR base/head/file-scope verification, verified remote merge and local-main synchronization.
- **Deletion boundary:** the detached local source-pack directory may be deleted only after merge and synchronization are verified; its deletion is local cleanup rather than application implementation evidence.
- **Unproven:** application framework/toolchain, dependencies, build, runtime, deployment and all PH-01+ behaviour.

### `BR-20260818-01`
- **Mode/scope:** PH-02 database-foundation implementation and local verification; `REQ-002`–`REQ-006`, `REQ-036`–`REQ-040`, `REQ-051`; partial `VAL-024`, `VAL-025`, `VAL-032`, `VAL-033`, `VAL-035`, `VAL-036`.
- **Implemented:** project-pinned `supabase@2.114.0`; version-controlled `supabase/config.toml`, migration, synthetic seed and pgTAP suite; profile/assessment-version/session schema; RLS ownership policies; row-version concurrency; immutable published assessment versions and completed assessment sessions.
- **Migration evidence:** local `supabase db reset` completed successfully from a clean local database and replayed the migration plus synthetic `seed.sql`.
- **Database-test evidence:** cached Supabase `pg_prove:3.36` image `sha256:eda7c5e68719e9c8287e78c017118407b48df904a51c935f5ab6098b8c0bc6bc` executed the repository test file; `Files=1, Tests=21`, `Result: PASS`.
- **Isolation evidence:** during migration/database verification the Windows adapter carrying the active IPv4/IPv6 default route was disabled; localhost API/DB remained reachable; Supabase was stopped before that adapter was restored. Persistent safe host exposure of the local development stack remains `Unproven` and is not relied upon.
- **Application regression evidence:** `npm run verify` passed after database verification: lint, TypeScript check, 2 Vitest tests, Next.js 16.3.1 production build and 12 Playwright tests.
- **CI follow-up in this slice:** CI adds an independent database job using the project-pinned CLI, local database startup, migration/seed replay and `supabase test db`; remote CI remains `Unproven` until the branch is pushed and GitHub Actions completes.
- **Not proven by this record:** account signup/sign-in/recovery/deletion, profile/assessment UI, save/resume safety behaviour, application server-side ownership checks, export/deletion lifecycle, remote Supabase, production region, persistent local-stack network isolation, production/runtime release behaviour.

### `BR-20260818-02`
- **Mode/scope:** PH-02 Auth/private-profile implementation and local runtime verification; partial `REQ-001`, `REQ-002`, `REQ-036`, `REQ-037`, `REQ-048`, `REQ-049`, `REQ-052`; partial `VAL-024`, `VAL-032`, `VAL-034`, `VAL-036`.
- **Implemented:** local Supabase Auth/account UI/actions, Auth callback/session integration, authenticated private-profile form/actions, validation modules/tests, optimistic profile conflict handling, dedicated Auth/profile Playwright integration, local environment helper and related CI/config changes.
- **Database continuity:** V4 reset/replayed the existing PH-02 migration and synthetic seed and reran the database suite successfully: `Files=1, Tests=21`, `Result: PASS`. Local Supabase ran with the active default-route adapter disabled and was stopped before network restoration.
- **Auth/profile runtime:** V5 reset the local database and `npm run test:e2e:auth` passed its Chromium flow (`1 passed`), exercising signup/sign-in/sign-out, private-profile persistence and a two-session stale-write conflict. The stale save produced the intended user-visible reload-before-saving message rather than overwriting the newer profile.
- **Test-harness correction:** V4's Auth/profile failure was a Playwright strict-mode locator ambiguity between the application `role="alert"` and Next.js' route announcer, not an application concurrency failure. V5 narrowed the assertion to the conflict alert text and the flow passed.
- **Generated-state correction:** V5's later full verification exposed ESLint scanning Supabase-generated `supabase/.temp/...` runtime code. V6 excluded `supabase/.temp/` and `supabase/.branches/` from Git and ESLint rather than modifying generated code.
- **Regression evidence:** V6 `npm run lint` passed and `npm run verify` passed, covering lint, TypeScript, Vitest, production build and the ordinary Chromium Playwright suite (`12 passed`). The Auth/profile test was not rerun in V6 because V6 changed only Git/lint discovery; its V5 pass remains the runtime evidence for that flow.
- **Still unproven:** password-recovery delivery/end-to-end update, account deletion, assessment save/resume/safety UI, export/correction/deletion lifecycle, complete negative cross-user application-authorisation coverage, remote Supabase, production region/hosting, remote GitHub Actions for this branch and release/runtime behaviour.

### `BR-20260818-03`
- **Mode/scope:** governance maintenance after PH-02 Auth/private-profile publication; no new application behaviour.
- **Remote PH-02 evidence:** GitHub Actions CI run #7 (`32109163956`) completed successfully for tested head `a14ba185795b8880973d800b3b43e15ca7b3099e`; both `Verify` and `Database and auth integration` completed successfully, including clean install, lint, typecheck, unit/component tests, production build, Chromium installation, local Supabase startup, environment generation, migration/seed replay, pgTAP and Auth/profile browser integration.
- **Merge evidence:** PR #6 was merged by exact-head match from `agent/ph02-auth-profile` into `main`; resulting merge commit `c5970dcb992ef385d57376e5fc5f5e30c118fb1f` was fetched and local `main` was fast-forwarded to the same SHA.
- **Operational-reference integration:** supplied `MEEXERCISE_FUTURE_INSTANCE_CHEATSHEET.md` was copied byte-for-byte to `project_docs/DEVELOPMENT_CHEATSHEET.md` (SHA-256 `2ecaf5f24527f7407e2918ebde2dece9f7f9a8674dd5053f0168e9f33c5419ad`) and routed from `AGENTS.md`, `PROJECT_INDEX.md` and `REPOSITORY_AND_RELEASE.md` as non-canonical history.
- **Integrity boundary:** the reference is intentionally excluded from `PACK_MANIFEST.json` and intentionally preserves the supplied source bytes, including inherited Markdown whitespace. Canonical changed governance/manifest files must pass diff-hygiene checks; reference integrity is verified by exact source/destination SHA-256 equality. `PROJECT_SETTINGS.md` is unchanged and remains below 8000 characters.
- **Evidence boundary:** this record proves PR #6 remote CI/merge publication and documentation integration only. PH-02 remains in progress; password recovery end-to-end, account deletion, assessment save/resume/safety, export/correction/deletion lifecycle, complete negative cross-user application-authorisation coverage, remote Supabase production configuration and release behaviour remain unproven.

### `BR-20260818-04`
- **Mode/scope:** PH-02 readiness-assessment save/resume/safety implementation and isolated local runtime verification; partial `REQ-004`, `REQ-005`, `REQ-006`, `REQ-007`; partial `AC-001`, `AC-002`, `AC-003`; `VAL-025` with supporting `VAL-032`, `VAL-034`, `VAL-036`.
- **Implemented:** versioned `/profile/assessment` flow; start/save/reload-resume/complete server actions; bounded readiness-response validation; optimistic session `row_version` conflict protection; updated published synthetic readiness template definition; immutable `assessment_safety_flags`; database-derived `restrict_generation` and `block_generation` outcomes; profile entry point; accessible native form controls; unit, pgTAP and authenticated Playwright coverage.
- **Safety boundary:** movement limitations derive a later-generation restriction; anything other than explicit independent-exercise `yes` plus professional-restriction `no` derives a block-generation/professional-input recommendation. Missing/uncertain readiness therefore fails closed. Flags do not diagnose, treat, certify medical safety or provide medical clearance. PH-04 generator consumption of these flags is not yet implemented, so `AC-003` remains only partially proven.
- **Static/regression evidence:** implementation continuation V3 completed with the exact 14-file source scope and `npm run verify` passed. This establishes the configured lint, TypeScript, Vitest, production-build and ordinary Playwright regression boundary only.
- **Runtime/database evidence:** assessment runtime verification V1 used project-local Supabase CLI `2.114.0`; external IPv4/IPv6 default-route isolation was verified during database/browser checks; local database reset replayed migrations plus synthetic seed; the repository pgTAP suite passed; authenticated Auth/profile/assessment browser integration passed; source integrity passed after cleanup; local Supabase was stopped and local environment/network state restored by the verification harness.
- **Persistence/history evidence:** users can save incomplete assessment state and reload/resume it; completion binds the session to its immutable template version and completed history cannot be rewritten by the application/database path. Derived safety flags are immutable and readable only through the owning authenticated user boundary.
- **Evidence boundary:** this record does not prove full onboarding (`AC-001`), interruption across every onboarding step (`AC-002`), later routine-generation enforcement of the safety flags (`AC-003`), password-recovery delivery/update, account deletion, export/correction/deletion lifecycle, complete negative cross-user application-authorisation coverage, remote Supabase, production region/hosting or production release behaviour. PH-02 remains in progress.
### `BR-20260818-05`
- **Mode/scope:** PH-02 readable user-data export and permanent account-deletion implementation with isolated local runtime verification; partial `REQ-001`, partial `REQ-038`, partial `AC-028`; `VAL-033` with supporting `VAL-024`, `VAL-034`, `VAL-036`.
- **Implemented:** authenticated `/profile/account` data/privacy controls; authenticated no-store `/profile/export` JSON response; export of account identifier/email, current private profile, assessment responses, derived safety flags and referenced assessment template/version definition; current-password re-authentication; exact `DELETE MY ACCOUNT` confirmation; server-only Supabase admin client; permanent Auth-user deletion; visible post-delete sign-in state; unit, pgTAP and authenticated Playwright coverage.
- **Privileged-secret boundary:** local Supabase CLI `SERVICE_ROLE_KEY` is written only to ignored `.env.local` as `SUPABASE_SERVICE_ROLE_KEY`. Runtime verification confirmed no `NEXT_PUBLIC_SUPABASE_SERVICE_ROLE_KEY` boundary and did not print the secret value. The admin client is server-only.
- **Static/regression evidence:** the exact 16-file source slice passed `npm run verify`, covering configured lint, TypeScript, Vitest, production build and ordinary Playwright regression checks.
- **Runtime/database evidence:** data-lifecycle runtime verification V1 used project-local Supabase CLI `2.114.0`; external IPv4/IPv6 default routes were disabled during database/browser checks; local database reset passed; the complete repository pgTAP suite passed including Auth-user cascade deletion of profile/session/safety-flag records; all authenticated Auth/profile/assessment/export/delete browser tests passed; source integrity passed after cleanup and local environment/network state was restored.
- **Export evidence:** the browser test downloaded `meexercise-data-export.json` and asserted account email, private display name, one readiness assessment, its version key, self-reported limitation response and derived movement-restriction safety flag. This proves readable export for the user-owned record types implemented as of this slice, not future PH-03+ data that does not yet exist.
- **Deletion evidence:** the browser test rejected an incorrect current password, accepted the correct password plus exact destructive phrase, returned the user to a deletion-confirmed sign-in surface and rejected sign-in using the deleted credentials. Database tests prove current MeExercise profile, assessment-session and assessment-safety-flag rows cascade when the Auth user is deleted.
- **Evidence boundary:** this record does not establish jurisdiction-specific retention exceptions/obligations, production privileged-secret handling, generic correction beyond currently editable profile fields, future-domain export coverage, password-recovery delivery/update, complete negative cross-user application-authorisation coverage, remote Supabase, production hosting/region or release behavior. `REQ-038` and PH-02 therefore remain partial/in progress.
### `BR-20260819-01`
- **Mode/scope:** PH-02 password-recovery implementation and isolated local runtime verification; partial `REQ-001`; `VAL-024` with supporting `VAL-033` and `VAL-036`.
- **Implemented:** version-controlled Supabase recovery-email template; server-side `/auth/callback` handling for recovery `token_hash`/`type=recovery`; response-bound Auth cookie mutation; canonical-site-URL redirect handling; existing authenticated password-update action; focused Mailpit-backed password-recovery Playwright coverage.
- **Static/regression evidence:** on `agent/ph02-password-recovery` at base `8cb0719c030508a82d2eabd6c342c3966268a0c4`, V9 verified the exact four-file source/config scope and `npm run verify` passed, covering configured ESLint, TypeScript, Vitest, Next.js production build and the ordinary Chromium Playwright suite. ESLint reported two existing unused-parameter warnings in `src/app/profile/assessment/actions.ts` and no errors.
- **Runtime/database evidence:** the local Supabase stack ran with active external IPv4/IPv6 default routes disabled; the local API/database/Mailpit endpoints remained reachable; `supabase db reset` replayed repository migrations/seed; the complete pgTAP repository suite passed (`Files=3`, `Tests=40`, `Result: PASS`); the dedicated authenticated Chromium suite passed all four flows after the final test-harness correction; local Supabase was stopped and the pre-existing `.env.local` plus network adapters were restored.
- **Recovery evidence:** the browser requested a reset without account-enumerating success text, captured the real local Auth recovery email through Mailpit, followed its token-hash recovery callback, remained on the configured `127.0.0.1:3000` application origin, reached the password-update form only with the recovery session, updated the password, reached `/profile`, signed out, observed rejection of the original password and then successfully signed in with the replacement password.
- **Failure-to-pass evidence:** early verification exposed an ES-target-incompatible test regex, a default recovery-link/SSR session handoff gap, request-origin drift from `127.0.0.1` to `localhost`, and a final Playwright retry bug caused by React Action reset of uncontrolled form fields. The final verified slice uses an ES-compatible parser, version-controlled token-hash template, server recovery verification with response-bound cookies, configured canonical-site redirects, and refills required sign-in fields after the deliberate old-password failure.
- **Evidence boundary:** this record proves local captured-email password recovery/update against the pinned local Supabase stack. It does not prove production SMTP/provider/domain deliverability, production hosting/region, broader negative application-layer authorisation, generic correction/retention obligations, remote Supabase or production release behaviour. PH-02 remains in progress.
### `BR-20260819-02`
- **Mode/scope:** PH-02 negative cross-user application-authorisation verification for currently implemented private surfaces; `REQ-002`, `REQ-036`; `RISK-027`; `VAL-024` with supporting `VAL-033` and `VAL-036`.
- **Implementation:** test-only. Added one authenticated Playwright scenario and changed no product authorisation code, schema, migration, dependency or runtime configuration.
- **Static/regression evidence:** on `agent/ph02-authorization-boundary` at base `0328cb86af067384dc02de95b2ac201820b215a2`, the exact one-file test scope passed `npm run verify`, covering configured lint, TypeScript, Vitest, production build and the ordinary Playwright regression suite.
- **Runtime/database evidence:** local Supabase ran with active external IPv4/IPv6 default routes disabled; database reset replayed repository migrations/seed; the complete pgTAP repository suite passed; the full authenticated Playwright suite passed; cleanup restored the prior `.env.local` and network state; source integrity remained one uncommitted test file.
- **Two-user adversarial evidence:** User A created a private profile and in-progress readiness assessment. User B, in a separate authenticated browser context, was given User A's real assessment session UUID and row version and tampered its own hidden form identifiers before saving. The application rejected the forged mutation through the existing ownership-scoped mutation path, and User A's assessment remained unchanged. User B's profile surface did not expose User A's profile. Each user's authenticated JSON export contained that user's own account/profile/assessment content and excluded the other user's email, profile and assessment markers.
- **Risk result:** the previously unproven negative cross-user application-authorisation boundary for the currently implemented PH-02 private profile, assessment mutation and export surfaces is now locally verified without additional product machinery. This result does not automatically cover future PH-03+ private modules; each new private surface must retain ownership-scoped tests.
- **Evidence boundary:** PH-02 remains in progress because `REQ-038` still needs an explicit correction/retention contract beyond currently editable profile fields. Production legal applicability, SMTP/deliverability, hosted Supabase, production hosting/region and release behaviour remain unproven/deferred as already governed.
### `BR-20260819-03`
- **Mode/scope:** PH-02 data-correction and primary-retention/deletion implementation with isolated local runtime verification; current PH-02 portion of `REQ-038`; `VAL-024`, `VAL-025`, `VAL-033`, `VAL-035`, `VAL-036`.
- **Implementation scope:** exact 16 source/config/test paths on `agent/ph02-data-correction` from base `bb4279fd4c6ddbc2c6ba47b3f610cfc7a25fc9c8`. No new service, generic rights subsystem, background worker or retention scheduler was introduced.
- **Correction contract:** current profile values and in-progress readiness answers remain editable records. Completed readiness assessments remain immutable; correction creates a new session linked by `corrects_session_id`, copies the source responses, preserves the same immutable template version and allows the successor to be completed with corrected values. Database tests verify the source cannot be rewritten and another authenticated user cannot create a correction against the owner's record.
- **Export evidence:** authenticated export advances to schema version 2 and includes `correctsSessionId`, preserving the relationship between historical source and corrected successor while retaining the assessment template/version context.
- **Account-email correction:** `/profile/account` accepts a different valid email only after current-password re-authentication. Local Supabase Auth requires confirmation at the new address before the account email changes. The browser flow captured the real local email-change message in Mailpit, followed its token-hash callback, observed the corrected account email/export, rejected sign-in with the old address and accepted the replacement address.
- **Deletion/retention evidence:** permanent Auth-user deletion cascades through the private profile, source and corrected assessment sessions and derived safety flags. PH-02 therefore retains no user primary record after permanent account deletion. No production backup-retention period or statutory exception is claimed; those depend on PH-10 jurisdiction/hosting/recovery decisions.
- **Static/regression evidence:** V5 `npm run verify` passed after the final test correction, covering configured lint, TypeScript, Vitest, production build and ordinary Playwright regression checks.
- **Runtime/database evidence:** external IPv4/IPv6 default routes were disabled during the local Supabase runtime checks; database reset replayed the complete migration chain plus synthetic seed; the full repository pgTAP suite passed; all six authenticated Playwright flows passed; local Supabase stopped and the prior environment/network state was restored; exact 16-path source integrity passed.
- **Failure-to-pass evidence:** V2 exposed two harness/contract issues: an ambiguous lifecycle password locator and a Supabase local Auth interaction where the attempted two-address email confirmation invalidated the second token. The local contract was narrowed to current-password re-authentication plus confirmation at the new address and the lifecycle locator was made exact. V3 then failed only because its continuation preflight incorrectly required the old config value. V4 reached the final correction assertions but timed out trying to use the `/profile` sign-out control while still on `/profile/account`. V5 navigated to the actual session-control surface and the complete suite passed.
- **Phase boundary:** PH-02 implementation scope is locally complete. Exact-head remote CI/publication/merge for this final slice remains unproven, so canonical PH-02 phase closure is deferred until that evidence is obtained. `REQ-038` remains cumulative for future PH-03+ user-owned record types. Production legal applicability, backups/restore/retention exceptions, remote Supabase, production hosting/data region and release behaviour remain PH-10 gates.
### `BR-20260819-04`
- **Mode/scope:** PH-02 exact-head remote-CI/merge verification and canonical phase closure; `VAL-021`, `VAL-022`, `VAL-023`, `VAL-024`, `VAL-025`, `VAL-032`, `VAL-033`, `VAL-034`, `VAL-035`, `VAL-036` as accumulated across the PH-02 evidence chain.
- **Publication evidence:** PR #12 contains one feature commit, exact head `10b5841bb9ea82e274c98a13e1bbf9ca0553249d`, exact base `bb4279fd4c6ddbc2c6ba47b3f610cfc7a25fc9c8` and the verified 24-file source/config/test/governance scope recorded by `BR-20260819-03`.
- **Remote CI evidence:** GitHub Actions run #19 (`32227451589`) is associated with that exact feature head. Its first integration attempt was cancelled during Playwright/Ubuntu Chromium dependency installation after package-mirror stalls, before Supabase/migrations/tests ran; `Verify` had already passed. A rerun on the unchanged feature head completed successfully. The current successful run attempt reports both `Verify` and `Database and auth integration` successful, including dependency install, lint, typecheck, unit/component tests, production build, Chromium install, local Supabase startup, local environment generation, migration/seed replay, complete pgTAP, authenticated Playwright integration and Supabase cleanup.
- **Merge evidence:** PR #12 was transitioned from draft to ready only after the exact-head CI result was green, then merged with the feature SHA guard. Merge commit `3a6c24c97f98df374f4b9ecd6ff59e826acecb07` has exact parents `bb4279fd4c6ddbc2c6ba47b3f610cfc7a25fc9c8` and `10b5841bb9ea82e274c98a13e1bbf9ca0553249d`. Returned PowerShell evidence verifies `origin/main` and local `main` both at the merge commit with a clean working tree.
- **Phase result:** `PH-02` is **Passed / Verified for the current identity/private-data foundation scope**. This result covers the implemented PH-02 account lifecycle, private profile, versioned readiness assessment, conservative safety flags, concurrency/lost-update protection, password recovery, application ownership boundary for current private surfaces, export v2, immutable-history correction, account-email correction and permanent deletion of current primary records.
- **Cumulative/future boundary:** `REQ-038` remains cumulative for user-owned record types introduced in PH-03+; each later domain must extend export/correction/deletion/retention coverage and ownership tests as applicable.
- **Still unproven / later gate:** production legal/privacy applicability, backup retention or statutory exceptions, production SMTP/deliverability, hosted production Supabase, production web hosting/data region, production secrets/transport exposure, restore drill, release smoke/rollback, support-matrix/manual assistive-technology claims and future routine-generator enforcement of the PH-02 safety flags remain outside this phase result.

### `BR-20260820-01`
- **Mode/scope:** first PH-03 exercise-content implementation and isolated local runtime verification; `REQ-008`, `REQ-009`, `REQ-010`, `REQ-011`; partial `AC-004`; `VAL-026` with supporting partial `VAL-034`.
- **Implementation scope:** exact nine source/test paths on `agent/ph03-exercise-library` from merged PH-02 closure base `22f0c923c46ae3be8cdd4c7b405afcc94314dfeb`. The slice adds stable exercise identities, immutable versioned structured instruction content, governed publication states, source-version relationship semantics, synthetic local seed content, a server-side read model and read-only `/exercises` browse/detail routes. No dependency, CI-workflow, professional-user access, routine generator, CMS or runtime-AI mechanism is added.
- **Content/version contract:** normal readers see only `general` and `reviewed` versions. Finalised instruction fields and finalised source-version relationships cannot be rewritten; allowed state transitions can review, withdraw or restrict a version without replacing its instructional snapshot. Stable identities and version numbers therefore provide the prerequisite for later exact-version routine references.
- **Static/build evidence:** after the V1 optional-relation TypeScript mismatch was corrected, V2 `npm run verify` passed: lint completed with only the two pre-existing PH-02 unused-parameter warnings, TypeScript passed, six Vitest files / eleven tests passed, the Next.js production build passed with `/exercises` and `/exercises/[exerciseKey]`, and the ordinary Chromium shell suite passed 12 tests. Later corrections changed only SQL/Playwright tests; publication preflight reran lint, typecheck and unit/component tests against the final working state.
- **Database evidence:** local Supabase CLI `2.114.0` ran with the active external default-route adapter disabled. Database reset replayed the full migration chain plus synthetic seed. The focused PH-03 pgTAP file passed all 13 planned assertions and the complete repository pgTAP suite passed. Local Supabase was stopped before network restoration and the prior `.env.local` was restored.
- **Browser evidence:** the focused exercise-library Playwright flow passed after narrowing the search locator to the semantic searchbox; the complete authenticated browser suite then passed. The flow verifies draft content is absent, visible exercise count/search/filter behaviour, structured detail content, plain-language text and a progression relationship link.
- **Failure-to-pass evidence:** V1 exposed an optional Supabase relation typing mismatch; an early continuation exposed an over-strict network-isolation adapter detector; early pgTAP attempts exposed `name`/`text` and then PostgreSQL collation failures in result-set comparison. A no-mutation diagnostic captured `could not determine which collation to use for string comparison` inside pgTAP `results_eq`; the final test uses scalar ordered enum aggregation with explicit `COLLATE "C"`. V6 then reached browser execution with focused/full pgTAP green; V7 corrected only the strict Playwright search locator and the browser suites passed.
- **Evidence boundary:** synthetic seed exercises prove mechanics only and are not claimed to be production-reviewed/professionally authored content. `VAL-026` is verified for PH-03 identity/version/publication/immutability mechanics, but its historical exact-routine-version portion remains unproven until PH-04 implements routine snapshots (`REQ-012`). `VAL-034`/`AC-004` are partial: semantic/text-only browser behaviour is exercised, while manual assistive-technology/usability judgement and release support-matrix evidence remain later gates. Exact-head remote GitHub CI/merge for this PH-03 slice is unproven at this record.
### `BR-20260820-02`
- **Mode/scope:** PH-03 acceptance-semantics implementation and local completion verification; `REQ-008`, `REQ-009`, `REQ-010`, `REQ-011`; `VAL-026` for the PH-03 exercise-content portion with supporting partial `VAL-034`/`AC-004`.
- **Starting remote evidence:** first-slice PR #14 contained exact feature head `bf5acd68dcdcd08cbe6fa29db9185459381184e0` on base `22f0c923c46ae3be8cdd4c7b405afcc94314dfeb`, exact 15-file scope, and GitHub Actions CI run #23 (`32338687733`) completed successfully on attempt 1 with both `Verify` and `Database and auth integration` passed. PR #14 then merged as `531fa19fb2b3117165a8618c2f672825be1246ff`; returned merge evidence verified exact parents, synchronized `origin/main`/local `main`, and a clean working tree.
- **Implementation scope:** exact five source/test paths on `agent/ph03-content-semantics` from merge base `531fa19fb2b3117165a8618c2f672825be1246ff`. No schema migration or dependency was needed because the first slice already modeled versions, publication states, relation types and side rules. The second slice extends the read contract and fixtures/tests rather than adding a CMS, service layer, professional-access mechanism or runtime AI.
- **Version/relation contract:** the library still selects the latest visible `general`/`reviewed` version for ordinary browsing. Exercise detail can also resolve a requested exact visible version. Relation links now include the target version number, so a finalised source-version relation cannot silently follow a later target version and change meaning. Synthetic fixtures prove two visible immutable versions under one stable identity, all four governed relation types, side-rule semantics and fallback to an earlier visible version after a later version is withdrawn.
- **Static/build evidence:** `npm run verify` passed on the exact five-path working state, covering configured lint, TypeScript, six Vitest files / eleven tests, Next.js production build and the ordinary Chromium Playwright shell suite. Existing PH-02 unused-parameter lint warnings remained non-fatal.
- **Database/runtime evidence:** local Supabase ran with the active external default-route adapter disabled; database reset replayed the full migration chain plus expanded synthetic seed; the complete repository pgTAP suite passed and the PH-03 file executed its exact 20/20 plan. The assertions cover RLS, publication/relationship/side-rule enums, visible-version filtering, multi-version stable identity, reviewed latest content, all four relation types, exact target-version persistence, finalised immutability, withdrawal and earlier-version fallback.
- **Browser evidence:** the full authenticated Playwright suite passed. The exercise-library flow verifies six visible identities, latest reviewed wall-push-up version selection, side-rule presentation, progression/equipment-alternative/substitution links, and navigation from an incline regression relation to the exact wall-push-up version 1 rather than the newer version 2.
- **Phase boundary:** the PH-03 implementation objective is locally complete. Production catalogue population/editorial review is not a PH-03 implementation gate: synthetic fixtures prove mechanics only and may not be presented as production-approved content. Before production routine generation or public release, consumed exercise versions must be approved under the applicable content-review/provenance boundary; professional authorship/reviewer provenance remains PH-09. `REQ-012` remains a PH-04 consumer contract because only persisted routine snapshots can prove exact historical exercise-version retention.
- **Still unproven:** exact-head remote CI/publication/merge for this final PH-03 implementation slice; canonical PH-03 phase closure; manual assistive-technology/support-matrix evidence; production catalogue content/review; PH-04 routine-snapshot retention; and PH-10 production legal/privacy/recovery/release gates.
### `BR-20260820-03`
- **Mode/scope:** PH-03 exact-head remote-CI/merge verification and canonical phase closure; accumulated PH-03 implementation evidence for `REQ-008`, `REQ-009`, `REQ-010`, `REQ-011`; `VAL-026` for the exercise-content portion with supporting partial `VAL-034`/`AC-004`.
- **First-slice publication evidence:** PR #14 carried feature head `bf5acd68dcdcd08cbe6fa29db9185459381184e0` from base `22f0c923c46ae3be8cdd4c7b405afcc94314dfeb`, exact 15-path publication scope and exact-head CI run #23 (`32338687733`) with `Verify` and `Database and auth integration` successful before guarded merge as `531fa19fb2b3117165a8618c2f672825be1246ff`.
- **Final-slice local evidence:** on `agent/ph03-content-semantics` from `531fa19fb2b3117165a8618c2f672825be1246ff`, exact five source/test paths passed `npm run verify`, database reset, the complete repository pgTAP suite with PH-03 plan 20/20, and the complete authenticated Playwright suite while external default routes were disabled. Cleanup restored Supabase, `.env.local` and network state.
- **Final-slice publication evidence:** PR #15 contained one commit, exact head `a4ac5905dfc558707cdb3b36e3d4ca97ee2bd1fc`, exact base `531fa19fb2b3117165a8618c2f672825be1246ff` and exact 12-path source/test/governance scope. GitHub Actions CI run #25 (`32364218196`) attempt 1 completed successfully on that exact head with both `Verify` and `Database and auth integration` successful, including production build, migration/seed replay, complete pgTAP and authenticated browser integration.
- **Merge evidence:** PR #15 was moved from draft to ready only after exact-head CI was green, then merged with the expected-head guard. Merge commit `acf82f7cc0548aa064772f105b7c7037018ca543` has exact parents `531fa19fb2b3117165a8618c2f672825be1246ff` and `a4ac5905dfc558707cdb3b36e3d4ca97ee2bd1fc`. Returned PowerShell evidence verifies `origin/main` and local `main` both at that merge commit with a clean working tree; GitHub independently reports PR #15 merged with the same head/base/merge SHA.
- **Phase result:** `PH-03` is **Passed / Verified for the exercise-content library scope**. The verified scope includes stable immutable exercise identities/versions, structured accessible-text instruction data, governed publication visibility, immutable finalised instruction and relationship semantics, latest-visible and exact-version reads, substitution/regression/progression/equipment-alternative relations, bilateral/side rules, withdrawal fallback, and read-only exercise browse/filter/detail surfaces.
- **Boundary retained:** synthetic exercise records prove mechanics only and are not production-approved catalogue content. Production catalogue population/editorial review remains a later activation/release content gate; production routine generation may consume only approved structured content. `REQ-012` remains a PH-04 consumer contract because historic routine retention requires persisted routine snapshots referencing exact exercise versions.
- **Still unproven / later gates:** manual assistive-technology and declared browser/device support-matrix evidence; production catalogue content/review; PH-04 routine snapshots and generation enforcement; production hosting/data region; legal/privacy applicability; recovery/restore; release smoke/rollback; and other PH-10 release evidence.
## Requirement mapping
| Requirement group | Primary validations |
|---|---|
| `REQ-001`–`REQ-007` | `VAL-024`, `VAL-025`, `VAL-033`, `VAL-034` |
| `REQ-008`–`REQ-012` | `VAL-026`, `VAL-034` |
| `REQ-013`–`REQ-019` | `VAL-027`, `VAL-026` |
| `REQ-020`–`REQ-024` | `VAL-028` |
| `REQ-025`–`REQ-030` | `VAL-029`, `VAL-030`, `VAL-034` |
| `REQ-031`–`REQ-035` | `VAL-031`, `VAL-033`, `VAL-034` |
| `REQ-036`–`REQ-040` | `VAL-032`, `VAL-033`, `VAL-035` |
| `REQ-041`–`REQ-047` | `VAL-038`, `VAL-024`, `VAL-033` |
| `REQ-048`–`REQ-052` | `VAL-023`, `VAL-034`–`VAL-039` as applicable |

## Minimum release evidence
A release claim must name the supported browser/device/deployment matrix and have evidence for the requirements actually included in that release. A green test suite does not prove unspecified browsers, scale, legal applicability, accessibility, backup restore, billing or professional workflows.

## Work report format
Use `BR-YYYYMMDD-##` for material repository-execution reports:
- mode/scope and affected IDs;
- inspected evidence;
- changed files/contracts/data;
- exact commands/environment;
- passed/failed/skipped/unproven;
- migration/data/rollback evidence where applicable;
- remaining risks/decisions.

### `BR-20260821-01`
- **Mode/scope:** PH-04 manual routine foundation first-slice implementation and isolated local verification; `REQ-012`, partial `REQ-013`, partial `REQ-019`, extended `REQ-038`; `VAL-026`, partial `VAL-027`, with supporting `VAL-024`, `VAL-033`, `VAL-034`, `VAL-036`.
- **Implemented:** private stable routine identities; immutable routine version-one snapshots; ordered section/item persistence; atomic authenticated manual creation from one to twelve unique currently approved exercise versions; readiness fail-closed creation boundary; owner-only `/plans` and `/routines/[routineId]`; manual `/create`; exact historical exercise-version retention after withdrawal for the routine owner only; export v3 routine history; Auth-user cascade deletion of routine data.
- **Historical/version evidence:** routine items store exact `exercise_version_id` values rather than stable exercise keys or latest-version lookups. A focused database test withdraws a referenced reviewed version and proves the owner can still read that exact withdrawn version through a separate authenticated historical policy while another authenticated user cannot. Routine version/section/item snapshot updates are rejected.
- **Safety/content evidence:** database creation rejects missing readiness, restricted/block-generation outcomes and non-visible exercise versions. The application independently rechecks readiness and current approved library content before the atomic database mutation. Free-text movement restrictions remain blocked rather than inferred; deterministic restriction matching/substitution remains later PH-04 work.
- **Lifecycle/privacy evidence:** routine RLS ownership and cross-user browser denial passed; export schema advanced from v2 to v3 and includes routine versions/sections/items plus exact exercise-version interpretation data; the existing assessment correction flow still exports correctly under v3; Auth-user deletion cascades through routine records.
- **Static/application evidence:** the V2 TypeScript empty-array inference defect was corrected; the V3 shell regression was a stale PH-01 heading assertion and the test was updated to the new `/create`/`/plans` H1 contracts. V4 `npm run verify` passed. After the final authenticated-test-only corrections, V5 lint and TypeScript checks passed.
- **Database evidence:** V3 exposed an RLS regression where replacing the PH-03 visibility policy made anonymous reads depend on private routine tables; V4 restored the PH-03 visible-content policy unchanged and added a separate authenticated-only historical-owner policy. V4 also corrected the PH-04 pgTAP plan from 19 to 22. Database reset passed; focused PH-03 pgTAP passed 20/20; focused PH-04 pgTAP passed 22/22; the complete repository pgTAP suite passed.
- **Browser evidence:** V4 found stale export-v2 assertions plus a test assumption that checkbox click order defined submitted routine order. V5 updates all correction-flow export assertions to v3 and verifies exact routine exercise title/version pairs independent of form-control order. Focused authenticated tests passed 2/2 and the full authenticated Chromium suite passed 8/8.
- **Isolation/cleanup evidence:** database and authenticated browser runtime checks ran with external default routes removed; local Supabase was stopped before the physical adapter and prior `.env.local` state were restored. The final source/test working scope before governance publication was exactly 15 paths and diff hygiene passed.
- **Passed / locally Verified:** `REQ-012` for saved routine snapshots; private routine ownership; approved-content/readiness fail-closed manual save; historical exact-version read for the owner after withdrawal; routine snapshot immutability; export-v3 inclusion; primary-record deletion cascade; PH-03 public-visibility regression; first owner-only routine list/detail flow.
- **Partial / Unproven:** `REQ-013` editing/new routine versions; deterministic movement-restriction matching and substitution; `REQ-014`–`REQ-016` guided generation/explanations/user review; broader `REQ-019`; unlimited templates; PH-04 exact-head remote CI/merge; production catalogue approval, legal/privacy/backup/recovery and release gates.

### `BR-20260823-01`
- **Mode/scope:** PH-04 ordered routine editing second slice; `REQ-012`, `REQ-013`, routine-update portion of `REQ-019`; `VAL-026`, partial `VAL-027`, supporting `VAL-024`, `VAL-025`, `VAL-032`, `VAL-033`, `VAL-034`, `VAL-035`, `VAL-036`.
- **Starting remote evidence:** first-slice PR #17 exact head `d426f35c4a5a6fc067ac473bfe57b217aa7064dc` passed CI run #29 (`32528696209`) with both required jobs and merged as `4fe2a2056303980992a9f88f3ef76cad4215879e`.
- **Implemented:** ordered create slots; owner-only edit route; atomic append-only N+1 version creation; stale expected-version rejection; approved historical-version carry-forward; withdrawn/restricted carry-forward rejection; correction-chain-leaf readiness authority shared by application/database.
- **Defect evidence:** V2 exposed timestamp-tied readiness ambiguity because PostgreSQL `now()` is transaction-stable. The fix resolves the highest published template version plus exactly one completed correction-chain leaf; competing leaves fail closed. The DB test uses a real linked correction successor.
- **Passed:** V3 `npm run verify`; database reset; focused routine-editing pgTAP 20/20; complete pgTAP suite. V4 changed only an overly broad Playwright alert locator; V4 lint/typecheck, focused authenticated routine test and full authenticated Chromium suite passed under external-network isolation.
- **Verified behaviours:** explicit order; immutable old/new routine versions; stale-edit conflict; cross-user edit denial; restrictive correction blocks edit; export v3 contains both versions; deletion cascades through multiple versions.
- **Unproven:** deterministic restriction matching/substitution, `REQ-014`–`REQ-016`, unlimited templates, second-slice remote CI/merge, PH-04 closure and PH-10 release/legal/recovery gates.

### `BR-20260824-01`
- **Mode/scope:** PH-04 deterministic structured-constraint prerequisite; extension of `REQ-005`; partial `REQ-010`, `REQ-014`, `AC-005`, `AC-006`, `VAL-027`; supporting `VAL-025`, `VAL-026`, `VAL-034`, `VAL-035`, `VAL-036`.
- **Starting remote evidence:** PR #18 head `84a30d52234ef32d8f39888ce3d413b699fc4271` is independently verified merged as `2ccd6fa867224d12737be878fa79c9cfacf658da` with 19 changed files. Its exact-head CI run #31 had both required jobs passed before merge. This slice starts from that merged main SHA.
- **Implemented:** published synthetic readiness template v2 with optional structured movement choices; preservation of readiness v1 historical interpretation/correction; exact exercise-version `constraint_tags` plus classification-complete state; finalised planning-metadata immutability; deterministic extraction of the current supported structured constraint set; exact approved-version compatibility testing; compatible target selection through existing version-owned substitution relations; server exercise reader exposure of planning classification.
- **Fail-closed boundary:** free text is not interpreted. Restricted v2 with no supported choice, `other_or_unclear`, legacy v1 restriction, block-generation readiness, ambiguous current history and unclassified constrained content do not become permissive planning state. Manual restricted create/edit remains blocked pending the consumer slice.
- **V1 verification:** `npm run verify` passed. Focused PH-03 exercise-content pgTAP passed 20/20 and PH-04 routine-editing pgTAP passed 20/20. The new structured-constraint focused suite passed 19/19 before the complete DB suite exposed one stale PH-02 regression fixture.
- **Regression-fixture correction:** PH-02 identity/assessment pgTAP had historically created a test-only draft readiness version using UUID `bbbbbbbb-bbbb-4bbb-8bbb-bbbbbbbbbbb2` and version number 2 and expected one visible published version. Once real readiness v2 existed, that fixture collided with both the primary key and `(template_id, version_number)` uniqueness contract. The test-only draft fixture was moved to UUID ending `bb9` / version 999 and its visible published-version expectation changed from one to two; product schema/content was not weakened.
- **V3 runtime evidence:** focused PH-02 identity/assessment pgTAP passed 21/21; structured-constraint pgTAP passed 19/19; the complete repository pgTAP suite passed; full authenticated Chromium integration passed. Runtime database/browser verification used external-default-route isolation and restored local Supabase, `.env.local` and the physical adapter after completion.
- **Browser evidence:** the real readiness flow can save/reload a supported structured movement choice on template v2 while retaining the current versioned correction semantics.
- **Passed / locally Verified:** structured readiness persistence; exact-version classification immutability; compatible substitution primitive; legacy/missing/blocked fail-closed behaviour; PH-02/PH-03/PH-04 regression continuity.
- **Partial / Unproven:** consumption by manual create/edit or guided generation; complete `REQ-014`–`REQ-016`; explanation/user replacement UI; production movement-constraint taxonomy and catalogue classification; this slice exact-head remote CI/merge; PH-04 closure and PH-10 release/legal/recovery gates.

### `BR-20260825-01`
- **Mode/scope:** PH-04 manual structured-constraint consumer; preserves `REQ-013`; partial `REQ-010`, `REQ-014`, `AC-003`, `AC-005`, `AC-006`, `VAL-027`; supporting `VAL-024`, `VAL-025`, `VAL-026`, `VAL-032`, `VAL-034`, `VAL-035`, `VAL-036`.
- **Starting remote evidence:** PR #19 head `209718787d316dd3d8de1aba52ca52bafe2f45b1` is independently verified merged as `a2075b7adeba8b07448e94dce881622e21cb0966`. Its exact-head CI run #33 passed both required jobs before merge. This slice starts from that merged main SHA.
- **Implemented:** application planning context for ready/supported-restricted/unresolved/blocked states; manual `/create` filtering to compatible approved exact versions; manual edit re-evaluation against current restrictions; reviewed existing-relation substitution suggestions; no automatic replacement; one private database constraint-consumption authority used by both routine create and append-only edit.
- **Database safety:** the complete submitted exact-version array is checked server-side/database-side before persistence. Unsupported/unapproved or conflicting selections fail before routine/version/item insertion. Supported restricted users can save a fully compatible manual routine; later current restrictions can require explicit replacement in a new routine version without rewriting old history.
- **Fail-closed boundary:** legacy version-1 restriction, missing/unclear structured choice, block-generation outcome, ambiguous/unavailable readiness and incompatible/unclassified constrained content remain non-permissive. Free text is never parsed into compatibility.
- **V1 static/prerequisite evidence:** `npm run verify` passed before isolated runtime. The previously merged structured-constraint focused pgTAP passed 19/19 in the V1 execution path.
- **V1 fixture failure:** the new consumer pgTAP initially failed because its synthetic assessment correction inserted already-modified responses. The existing PH-02 correction trigger correctly requires a correction to be inserted as an exact copy of the completed predecessor responses.
- **V2 fixture correction:** only `supabase/tests/ph04_constraint_consumption.test.sql` was corrected so the successor first copies predecessor `user_id`, exact `template_version_id` and `responses`, then edits/completes the in-progress successor. Product code/schema was unchanged by V2.
- **V2 runtime evidence:** database reset passed; focused constraint-consumption pgTAP passed 15/15; routine-foundation regression passed 22/22; routine-editing regression passed 20/20; complete repository pgTAP passed; full authenticated Chromium integration passed. Runtime verification used external-default-route isolation and restored local Supabase, `.env.local` and the physical adapter afterward.
- **Browser evidence:** unrestricted ordered manual create/edit/history remains functional; a supported `surface_hand_loading` restriction filters incompatible options, displays an existing compatible substitution suggestion, permits explicit user selection of the standing resistance-band press, and later constrained editing creates a new version without rewriting prior versions.
- **Passed / locally Verified:** manual structured restriction consumption in create/edit, complete-selection database validation, explicit user-selected compatible substitution, historical immutability and existing ownership/stale-write regressions.
- **Partial / Unproven:** guided deterministic proposal construction, `REQ-015` explanation, `REQ-016` per-item generated review/replacement before activation, unlimited templates, production constraint taxonomy/catalogue review, this slice exact-head remote CI/merge, PH-04 closure and PH-10 release/legal/recovery gates.

### `BR-20260910-01`
- **Mode/scope:** PH-04 deterministic guided proposal/explanation/review implementation and local verification; partial `REQ-014`; current-surface evidence for `REQ-015`, `REQ-016`, `AC-006`, `AC-007`, `VAL-027`; supporting `VAL-023`, `VAL-025`, `VAL-034`, `VAL-036`.
- **Starting remote evidence:** PR #20 head `f67fd24b71f110cc995330085048a8bb61ca22c0` is independently confirmed merged as `54ce10def372cdb63df3d25bbb5524d940a70511`; this branch starts from that exact merged main SHA.
- **Implemented:** pure deterministic balanced/upper/lower candidate selection; bounded 1–6 item count; proposal-level purpose/balance/constraint/substitution explanations; reviewed purpose/summary/target-area/equipment per item; explicit replacement selector for every item; same-primary-target-area replacement options; compatible substitution notes; final reviewed save through existing authoritative routine mutation.
- **Non-authority boundary:** proposals are ephemeral. Runtime AI, free-text interpretation and automatic substitution are absent.
- **Failure/correction chronology:** initial full browser tests persisted both guided routines but the test populated the manual builder's duplicate-label title field. Narrowing that locator exposed a real duplicate HTML ID shared by the guided heading and review-title input. The input ID and associated test locators were corrected; final verification passed.
- **Development-machine transfer:** the repo moved to another Windows PC at the same `C:\Apps\Meexercise` path. Fresh Docker lacked required Supabase images, so an isolated first `supabase start` could not resolve external registries after WiFi was disabled. Required images were warmed once online; the stack was stopped; WiFi was then disabled and Supabase restarted successfully from cache for the actual verification.
- **Final local evidence:** `npm run verify` passed; online Supabase cache warm-up passed; offline cached Supabase start passed; DB reset passed; focused guided authenticated E2E passed; full authenticated E2E passed; external-network isolation was verified; Supabase was stopped and WiFi plus original `.env.local` restored.
- **Passed / locally Verified:** bounded deterministic proposal generation from approved compatible exact versions; purpose/balance/constraint/substitution explanations; explicit review/replacement of every proposed item; supported structured-restriction exclusion; reviewed-selection persistence through authoritative routine mutation; corrected unique form-control ID semantics.
- **Partial / Unproven:** full `REQ-014` consumption of goals/preferences/equipment/facilities/available-time/frequency; unlimited templates (`REQ-018`, `AC-011`); production catalogue/taxonomy review; exact-head remote CI/merge for this slice; PH-04 closure and later release gates.

### `BR-20260910-02`
- **Mode/scope:** PH-04 structured planning-profile prerequisite implementation and isolated local verification; `REQ-003`; supporting `REQ-002`, `REQ-037`, `REQ-038`, `AC-001`, `AC-028`, `VAL-023`, `VAL-024`, `VAL-032`, `VAL-033`, `VAL-035`, `VAL-036`.
- **Starting remote evidence:** PR #21 exact head `d458f9dab5811a2dbfa243c48e274e6f2ceebcd9` passed exact-head GitHub Actions run #37 and merged into `main` as `65f450e624cdb8b2414382afab8a05a0cbf3449b`; this branch starts from that exact merged SHA.
- **Implemented:** bounded private structured profile capture for primary/secondary goal priority, preferred methods, available equipment, available facilities, available minutes per routine and preferred routine days per week; server parsing/completeness contract; existing owner RLS and optimistic `row_version` mutation reused; partial saves retained; readable export advanced to v4 with planning-profile content.
- **Migration/schema evidence:** forward local reset replayed `20260910130000_ph04_planning_profile.sql` with all prior migrations and synthetic seed. Database constraints reject unsupported/duplicate/contradictory planning values and invalid numeric bounds while retaining the existing profile owner/concurrency policy.
- **Test-harness corrections:** initial isolated pgTAP execution on the transferred PC could not start because the separate `supabase/pg_prove:3.36` runner image had not been cached before WiFi isolation. The runner was then cached online under the supported registry aliases. The focused SQL test also declared `plan(22)` while containing 24 assertions; that accounting declaration was corrected without changing product behaviour.
- **Migration correction:** the first migration draft placed a private array-uniqueness helper inside profile `CHECK` constraints and revoked execute permission from the authenticated writer. That unnecessary callable dependency was removed; final uniqueness enforcement is self-contained in the finite-vocabulary table constraints.
- **Static evidence:** final `npm run verify` passed after the migration correction.
- **Focused database evidence:** planning-profile pgTAP passed 24/24; existing PH-02 profile/RLS/concurrency regression passed 21/21.
- **Complete database evidence:** `npm run test:db` passed the complete repository pgTAP suite after full migration/seed replay.
- **Authenticated browser evidence:** `npm run test:e2e:auth` passed. The profile flow persisted structured planning choices across reload and sign-out/sign-in, retained the planning values when a concurrent stale profile save was rejected, export v4 included the new profile fields, and existing deletion/correction/routine export regressions passed.
- **Isolation evidence:** after online dependency-image acquisition, the physical default-route adapter was disabled; local Supabase/database/browser verification completed with external-network isolation; Supabase was stopped and WiFi plus the original `.env.local` were restored.
- **Passed / locally Verified:** `REQ-003` structured current-profile capture; owner-private persistence; profile stale-write protection for the expanded row; database validation of accepted planning vocabulary/bounds; export-v4 inclusion of the current structured planning profile.
- **Partial / Unproven:** the deterministic generator does not yet consume the structured planning profile; exercise versions do not yet own all explicit planning metadata needed to honour those inputs; unlimited templates (`REQ-018`, `AC-011`); this slice exact-head remote CI/merge; PH-04 closure and production/release gates.

### `BR-20260917-01`
- **Mode/scope:** PH-04 exact-version exercise planning-metadata prerequisite implementation and isolated local verification; advances `REQ-008`, `REQ-009` and the prerequisite boundary for `REQ-014` / `AC-006` / `VAL-027`.
- **Starting remote evidence:** PR #22 exact head `b6f72a292d954fc1b33392127b10d9e71142aa1c` is merged into `main` as `4dbb02e00368cd435012c1fe18e18922d5b90f06`; this branch starts from that exact merged SHA.
- **Implemented:** exact-version planning goal tags, method tags, equipment tokens, facility tokens, estimated minutes and explicit planning-metadata completeness on `exercise_versions`; finalisation requires complete planning classification; finalised planning metadata is immutable; server exercise library exposes the structured fields; synthetic visible fixtures receive explicit planning classification.
- **Inference boundary:** metadata is authored explicitly and is not derived from exercise titles, instructions, target areas, user free text or runtime AI. Synthetic classifications prove mechanism only.
- **Test-harness correction:** the focused pgTAP file declared 18 assertions while containing 17. Product/migration code was preserved and only `plan(18)` was corrected to `plan(17)`.
- **Static evidence:** final continuation `npm run verify` passed.
- **Migration evidence:** local database reset replayed the full migration chain plus synthetic seed successfully.
- **Focused database evidence:** exercise-planning-metadata pgTAP passed 17/17; existing PH-03 exercise-content regression passed 20/20.
- **Complete database evidence:** `npm run test:db` passed the complete repository pgTAP suite.
- **Authenticated browser evidence:** `npm run test:e2e:auth` passed.
- **Isolation evidence:** verification ran with the physical default-route adapter disabled after cached dependency acquisition; local Supabase remained reachable, then was stopped and WiFi plus the original `.env.local` were restored.
- **Passed / locally Verified:** planning vocabularies/bounds, explicit completeness, draft-finalisation gate, finalised metadata immutability, seed classification mechanics, library exposure and regression coverage.
- **Partial / Unproven:** deterministic guided generation does not yet consume the complete structured profile; production catalogue/taxonomy breadth and editorial review; unlimited templates (`REQ-018`, `AC-011`); this slice exact-head remote CI/merge; PH-04 closure and production/release gates.

### `BR-20260918-01`
- **Mode/scope:** PH-04 profile-aware deterministic guided-generation consumption and isolated local verification; consumes `REQ-003` for the current guided generator and advances/local-verifies `REQ-014`, `REQ-015`, `REQ-016`, `AC-006`, `AC-007` and `VAL-027`.
- **Starting remote evidence:** PR #23 exact head `100614e3d6c8c92df9b0425fc08c5b2e050e4076` merged into `main` as `2e9652871f6fc53a84dec85af275f69c6860e790` after exact-head CI run #41 passed both required jobs. The current branch starts from that exact merged SHA.
- **Implemented:** guided generation requires a complete current structured planning profile; primary goal is a hard exact-version filter; secondary goal is a deterministic tie-break; preferred methods and facilities require intersection; required equipment must be available; summed exact-version estimated minutes must fit the routine-time budget; weekly frequency is included as planning context without a scheduling claim.
- **Safety/approval composition:** the consumer operates only after existing current-readiness and structured movement-constraint checks. Candidate input remains current approved exact exercise versions. Runtime AI and free-text inference are absent.
- **Review/persistence boundary:** every generated item remains explicitly reviewable. Replacement choices preserve target-area structure and are filtered so the whole reviewed proposal stays within the same planning/time constraints. A guided-specific server wrapper rechecks the current profile/current library before delegating to the existing routine mutation; a profile changed after proposal generation therefore fails closed before save.
- **Manual-boundary preservation:** manual routine creation remains user-directed and continues to enforce readiness/approval/movement constraints without treating preference fields as mandatory safety constraints.
- **Static evidence:** `npm run verify` passed on the exact six-path source/test slice.
- **Migration/runtime setup evidence:** local `db:reset` successfully replayed the merged migration/seed baseline. This slice adds no migration.
- **Focused browser evidence:** authenticated routine-builder E2E passed, covering incomplete-profile guided blocking while manual creation remains available, complete-profile proposal/explanation, structured-restriction generation/review, and stale-profile rejection before guided persistence.
- **Complete browser evidence:** the full authenticated E2E suite passed after the focused run.
- **Isolation evidence:** database/browser verification ran with the physical default-route adapter disabled; local Supabase remained reachable and was stopped before WiFi and the original `.env.local` were restored.
- **Passed / locally Verified:** deterministic profile consumption, complete-profile gate, approved/readiness/constraint composition, total routine-time budget, explanation, review/replacement compatibility and stale-profile save revalidation.
- **Partial / Unproven:** exact-head remote CI/merge for this slice; unlimited templates (`REQ-018`, `AC-011`); production exercise-catalogue breadth/review; PH-04 closure and production/release gates.

### `BR-20260919-01`
- **Mode/scope:** PH-04 reusable routine-template implementation, isolated local verification and closure audit; advances the template portion of `REQ-018` / `AC-011`, with supporting `REQ-012`, `REQ-013`, `REQ-019`, `VAL-026`, `VAL-027`, `VAL-024`, `VAL-033` and `VAL-036`.
- **Starting remote evidence:** PR #24/profile-aware guided generation is merged into `main` as `ddb57a47ae5669e8d7ef575510f928a8a605aff1`; the current branch starts from that exact merged SHA.
- **Implemented:** owner-private `routine_templates` pinned to one immutable source routine version; save-from-routine and create-routine-from-template authenticated mutations; `/plans` template save/create controls; server template reads/export; account export v5 template metadata; account-deletion cascade.
- **Version/safety boundary:** later source routine edits do not mutate the saved template. Instantiation reuses the saved exact exercise-version ordering but delegates to the current authoritative manual-routine mutation, so current readiness, approval and structured movement constraints are rechecked before a new routine can be persisted. Historical withdrawn exact content remains readable but fails closed for new instantiation.
- **Free/unlimited boundary:** no subscription/entitlement mechanism or template count gate was added. Focused pgTAP creates 26 templates for one owner and verifies cross-user isolation. This proves the routine-template portion only; saved plans are still absent, so full `REQ-018` / `AC-011` remain partial.
- **Test-harness correction:** the first focused pgTAP declaration planned 20 tests while executing 22. The corrected suite separately verifies authenticated table-update denial (`42501`) and the privileged immutable-snapshot trigger (`55000`), producing a final 23-test plan without weakening table privileges.
- **Runtime defects corrected before final evidence:** stale `/plans` heading expectations and encoding-sensitive E2E labels were corrected; a real Next.js contract error was then exposed because a `"use server"` file exported a non-async initial-state object. The final implementation keeps only async runtime exports in the server-action file and moves initial action state to the client component.
- **Database evidence:** full migration/seed replay passed with `20260918100000_ph04_routine_templates.sql`; focused template pgTAP passed 23/23; the complete repository pgTAP suite passed. These database results precede the later presentation/server-action-only corrections, which did not modify migration or SQL-test semantics.
- **Final static/browser evidence:** V8 `npm run verify` passed; a fresh local database reset passed; focused authenticated routine-builder E2E passed; the complete authenticated E2E suite passed.
- **Isolation evidence:** final runtime verification ran with the physical default-route adapter disabled; local Supabase remained reachable, then Supabase was stopped before WiFi and the original `.env.local` were restored.
- **Passed / locally Verified:** immutable exact source snapshot, owner isolation, current-boundary instantiation, export v5 inclusion, account-deletion cascade, no template count/subscription gate, and authenticated browser save/create flows.
- **Closure audit:** PH-04 is not complete. Canonical PH-04 explicitly requires plan/routine snapshots, while repository search/current implementation contain routine snapshots but no durable `plans` / `plan_versions` authority. PH-05 owns recurrence/scheduling/progression behaviour, so the remaining PH-04 slice is the durable versioned plan-composition foundation, not premature PH-05 mechanics.
- **Unproven:** template-slice exact-head remote CI/merge; durable plan snapshots and the plan portion of `REQ-018` / `AC-011`; full `REQ-017`; PH-04 closure; production catalogue/release/legal/recovery gates.
### `BR-20260919-02`
- **Mode/scope:** PH-04 durable versioned plan-composition foundation, isolated local verification and local implementation closure; locally completes the saved-plan portion of `REQ-018` / `AC-011`, establishes the PH-04 composition portion of `REQ-017`, and supports `REQ-012`, `REQ-019`, `VAL-026`, `VAL-027`, `VAL-024`, `VAL-032`, `VAL-033`, `VAL-035` and `VAL-036`.
- **Starting remote evidence:** PR #25/reusable routine templates is merged into `main` as `d4bebba1a535dfe321b43c17b4f1638b87021a6f` after exact-head CI run #45 passed; `agent/ph04-plan-snapshots` starts from that exact merged SHA.
- **Implemented:** stable owner-private `plans`; immutable append-only `plan_versions`; ordered exact `routine_version` composition links; authenticated create/append-version mutations with stale expected-version rejection; current plan list/detail/edit server readers; `/plans` create/list UI; `/plans/[planId]` exact-snapshot detail; `/plans/[planId]/edit` append-only editing; account export v6 plan history; account-deletion cascade.
- **Version/safety boundary:** later routine edits do not mutate existing plan versions. Every new plan/version resolves the exercises inside the submitted exact routine snapshots and reuses the existing authoritative readiness/approval/structured-constraint validator before persistence. Historical plan snapshots remain readable after later content withdrawal, while newly saved composition fails closed if a selected snapshot no longer satisfies the current boundary.
- **Concurrency/ownership boundary:** the stable plan row is locked for append; stale expected versions fail instead of silently overwriting. Owner RLS and server ownership checks isolate plan reads/mutations; authenticated direct table writes are revoked and immutable-update protection remains defence in depth.
- **Free/unlimited boundary:** no subscription/entitlement mechanism or plan-count gate was added. Focused pgTAP creates 26 plans for one owner and verifies owner isolation. Together with the merged template slice this locally verifies `REQ-018` / `AC-011` for PH-04.
- **Database evidence:** full migration/seed replay passed with `20260919120000_ph04_plan_snapshots.sql`; focused plan-snapshot pgTAP passed its 30-test plan; the complete repository pgTAP suite passed.
- **Browser evidence:** the focused authenticated plan flow passed after test-source encoding repairs, proving exact routine-version pinning after a source routine edit, append-only plan versioning to the newer routine snapshot, export-v6 history and cross-user plan-detail denial. The complete authenticated E2E suite then passed.
- **Static evidence:** `npm run verify`, `git diff --check` and TypeScript checks passed on the implementation. The final isolated run repeated `npm run verify`.
- **Isolation evidence:** an elevated verification run disabled every active Windows default-route adapter, verified an external HTTPS probe was blocked, confirmed local Supabase API `127.0.0.1:54321` and DB `127.0.0.1:54322` remained reachable, then passed offline `npm run verify`, full migration replay, complete pgTAP and complete authenticated E2E. Supabase was stopped before adapters were re-enabled and the original `.env.local` was restored.
- **Harness/test corrections before final evidence:** generated patch matching was changed to newline-normalized comparison; App Router `[planId]` file checks use literal paths; the Supabase status probe no longer treats expected native stderr from a stopped stack as a terminating PowerShell 5.1 error; generated E2E mojibake for em dash and middle dot was repaired without product-behaviour changes.
- **Passed / locally Verified:** durable immutable plan-version authority, exact ordered routine-version composition, stale-write rejection, current-boundary revalidation, owner isolation, historical exact-version retention, export v6, account-deletion cascade, no saved-plan count/subscription gate and the PH-04 completion boundary.
- **Phase boundary:** PH-04 implementation scope is locally complete. Full `REQ-017` remains partial because recurrence, timezone-aware scheduling and progression are deliberately PH-05. Exact-head remote CI/merge for this final slice and canonical remote PH-04 phase closure remain Unproven.
- **Later Unproven gates:** production exercise-catalogue review/provenance, production hosting/data region, jurisdiction-specific privacy/legal requirements, restore/release evidence and other PH-10 gates.
### `BR-20260919-03`
- **Mode/scope:** PH-04 exact-head remote verification and canonical phase closure; closes the defined PH-04 routine-and-plan-builder scope after `BR-20260919-02`.
- **Exact tested head:** `e5958060482b4c1d3ea8a86b63deb32267ac41d8`.
- **Remote CI evidence:** GitHub Actions CI run #47 (`35420487769`) completed successfully for that exact head. Required jobs `Verify` and `Database and auth integration` both completed successfully; the latter included local Supabase start, environment generation, migration/seed replay, complete pgTAP, authenticated browser integration and Supabase cleanup.
- **PR/merge evidence:** PR #26 moved out of draft only after exact-head CI was independently verified green and merged the exact tested head into pre-merge base `d4bebba1a535dfe321b43c17b4f1638b87021a6f`. GitHub records merge commit `4ee47affe049683de09b478fc13eedfb11397e94` at `2026-09-19T04:17:53Z`.
- **Mainline evidence:** GitHub `refs/heads/main`, returned local `origin/main`, and returned local `main` all resolve to `4ee47affe049683de09b478fc13eedfb11397e94`; the local working tree was clean after fast-forward.
- **Publication path:** the GitHub connector successfully verified read state but its ready-for-review write returned `Resource not accessible by integration`; the established local authenticated `gh` fallback was used with exact-head guards. This is an integration permission limitation, not a repository or CI failure.
- **Passed / Verified:** PH-04 manual and deterministic guided routine generation/review, immutable routine versioning, structured constraint/planning authorities, reusable templates, durable immutable plan composition, current-boundary revalidation, stale-write rejection, owner isolation, export v6, account-deletion cascade, and no plan/template count/subscription gate for the implemented scope.
- **Phase closure:** PH-04 is Passed / Verified for its defined routine-and-plan-builder scope. Full `REQ-017` remains partial because recurrence, timezone-aware scheduling and progression are explicitly PH-05 responsibilities rather than missing PH-04 work.
- **Still Unproven / later gates:** production exercise catalogue review/provenance, production hosting/data region, jurisdiction-specific privacy/legal applicability, restore/release evidence, support-matrix/manual assistive-technology claims and other PH-10 requirements.

### `BR-20260923-01`
- **Mode/scope:** PH-05 first scheduling slice, versioned weekly schedule authority and local verification; advances the schedule portion of `REQ-017`, partially implements `REQ-020` / `AC-012`, and provides partial `VAL-028` evidence plus applicable `VAL-032`, `VAL-033`, `VAL-035` and `VAL-036` coverage.
- **Starting authority:** verified merged `main` baseline `2dda09e15780cbfea7eba75cec3789a4e993a3af`; work remains uncommitted on `agent/ph05-schedule-foundation` at the time of this record.
- **Implemented:** stable owner-private `plan_schedules`; immutable append-only `plan_schedule_versions` pinned to exact `plan_versions`; immutable weekday/local-time `plan_schedule_rules` pinned to exact `routine_versions`; recognized named timezone storage; bounded occurrence projection; expected plan/schedule version stale-write rejection; versioned pause; `/plans/[planId]/schedule`; plan-detail schedule/upcoming state; Today next-occurrence projection; account export v7.
- **Safety/ownership boundary:** schedule mutation accepts only exact routine versions from the current exact plan version and reuses the current routine/readiness/approval/structured-constraint validator. Owner-only RLS plus application ownership checks protect reads; direct authenticated table writes are revoked; another authenticated user receives the non-disclosing unavailable schedule state.
- **Timezone evidence:** focused database tests verify weekly local 09:00 recurrence in `Australia/Adelaide` across the 2026 daylight-saving transition while the corresponding UTC projection changes, proving named-timezone/local-wall-clock semantics rather than fixed-offset recurrence.
- **Concurrency/history evidence:** plan and schedule expected-version checks reject stale writes. Historical schedule version 1 remains pinned to plan version 1 after plan version 2 exists; a later schedule version can deliberately pin plan version 2 without rewriting the earlier schedule snapshot.
- **Pause/lifecycle evidence:** pause is represented by another immutable schedule version and produces no current upcoming occurrences. Account deletion initially exposed a real restrictive foreign-key defect on `plan_schedule_rules.routine_version_id`; the final migration uses `ON DELETE CASCADE`, after which focused deletion/orphan assertions pass.
- **Static evidence:** after correcting an effect initialization lint defect and updating the Today unit test to mock the server-only schedule boundary, `git diff --check` and full `npm run verify` passed for the exact 14-path implementation/test scope.
- **Database evidence:** clean local migration plus synthetic-seed replay passed; Supabase database lint at error level passed; focused `ph05_schedule_foundation.test.sql` passed its 33-test contract; the complete repository pgTAP regression passed.
- **Authenticated browser evidence:** the complete authenticated Playwright suite passed, including schedule creation, exact version display, Today projection, stale two-page schedule-write rejection, versioned pause, export-v7 history and cross-user schedule denial.
- **Environment evidence:** the runtime harness preserved/restored the prior `.env.local`, started local Supabase only when required, stopped it in cleanup, and the post-run exact 14-path source audit plus `git diff --check` passed.
- **Passed / locally Verified:** first weekly recurrence foundation, named-timezone projection, immutable exact plan/routine schedule snapshots, stale-write rejection, versioned pause, owner isolation, export v7 and account-deletion cascade.
- **Still Unproven / incomplete:** remote exact-head CI, commit/push/PR/merge; per-occurrence skip/reschedule; reminder delivery; progression proposal/reversal and missed-session/difficulty/discomfort responses; full `REQ-020`, `AC-012`, `VAL-028` and PH-05 closure; production/release/legal/recovery gates.

### `BR-20260923-02`
- **Mode/scope:** PH-05 per-occurrence schedule-exception slice and local runtime verification; further advances the schedule portion of `REQ-017`, `REQ-020`, `AC-012` and `VAL-028`, with applicable `VAL-032`, `VAL-033`, `VAL-035` and `VAL-036` coverage.
- **Prior remote publication evidence:** first-slice head `a4ab750756bf58c310e66055d527668a392ea9d5` passed GitHub Actions CI run #51 (`35808748743`). Both required jobs, `Verify` and `Database and auth integration`, completed successfully; PR #28 merged that exact head into `main` as `10e4818f4a27bd78dc1d93b213f2eeac31f6adff`.
- **Starting authority for this slice:** verified merged `main` baseline `10e4818f4a27bd78dc1d93b213f2eeac31f6adff`; work remains uncommitted on `agent/ph05-schedule-exceptions` at the time of this record.
- **Implemented:** stable exception identity for one exact schedule rule plus original local date; immutable append-only skip/reschedule/restore versions; expected current schedule and exception version checks; exception-aware occurrence projection; current exception-management UI on `/plans/[planId]/schedule`; reschedule explanation on plan detail and Today; account export v8 with complete exception history.
- **History semantics:** reschedule changes only effective local date/time while preserving the exact routine version and schedule timezone from the original rule. A newly saved recurring schedule version has a fresh exception set by design; prior exception history remains attached to its exact earlier schedule snapshot and is not silently inherited.
- **Ownership/concurrency evidence:** owner RLS and server/database ownership checks protect exception reads/mutations. Direct authenticated table writes are revoked and privileged rewrites are blocked by immutable-snapshot protection. Stale expected exception versions return the existing concurrency failure path instead of overwriting a newer decision. Another authenticated user cannot read or mutate the owner's occurrence exceptions.
- **Database evidence:** clean migration plus synthetic-seed replay passed with `20260923024102_ph05_schedule_exceptions.sql`; Supabase database lint at error level passed; focused `ph05_schedule_exceptions.test.sql` passed its 39-test contract; complete repository pgTAP regression passed.
- **Focused semantics evidence:** pgTAP verifies baseline occurrence projection, skip suppression, stale exception rejection, skip-to-reschedule version append, rescheduled effective date/window, original local date retention, exact routine-version preservation, explicit rescheduled status/exception version, restore behaviour, invalid weekday/window rejection, fresh exception semantics after schedule version N+1, cross-user denial, immutable rewrite rejection and account-deletion cascade/no-orphan behaviour.
- **Authenticated browser evidence:** the complete authenticated Playwright suite passed, including skip, restore, reschedule, Today effective projection/explanation, creation of a new recurring schedule version without exception inheritance, export-v8 exception history and cross-user schedule denial.
- **Static/environment evidence:** the exact 12-path implementation/test scope passed `git diff --check` and full `npm run verify`; runtime verification preserved/restored the prior `.env.local`, started/stopped local Supabase only as required, and produced no post-run source-scope drift.
- **Passed / locally Verified:** append-only occurrence skip/reschedule/restore authority, stale-write protection, exact snapshot/routine retention, exception-aware projection, owner isolation, export v8 and account lifecycle behaviour.
- **Still Unproven / incomplete:** exact-head remote CI, commit/push/PR/merge for this second slice; reminder capability; progression proposal/reversal and missed-session/difficulty/discomfort responses; full `REQ-020`, `VAL-028` and PH-05 closure; production/release/legal/recovery gates.

### `BR-20260923-03`
- **Mode/scope:** PH-05 optional in-app schedule reminders; `REQ-020` reminder-setting and app-view portion, scheduling portion of `REQ-017`, partial `VAL-028`, applicable owner/lifecycle and export checks. No external delivery claims.
- **Prior publication:** second-slice head `e34b807ad66b374da4540fca648ff84ab087a6e9` passed both `Verify` and `Database and auth integration` jobs in exact-head CI run #53 (`35822901819`) and PR #29 merged as `8416a43472fabeb141006a172f9e2e8a42df51e7`.
- **Starting authority:** merged baseline `8416a43472fabeb141006a172f9e2e8a42df51e7`; reminder code and tests are locally applied/uncommitted on `agent/ph05-schedule-reminders` when this record was written.
- **Implemented:** optional immutable per-schedule-version reminder lead time (15 to 10080 minutes), transactional schedule save, owner RLS and direct-write denial, immutable historical reminder settings, effective-occurrence `reminder_at` projection, server-computed `reminderDue`, editor/plan/Today display and export v9 with historical settings.
- **Regression and exceptional behavior:** the existing schedule-save RPC and the first 18 occurrence output columns remain intact. Skip suppresses the reminder, reschedule moves its timestamp without retargeting the exact routine version, restore reinstates it, version N+1 may disable reminders without rewriting historical settings and account deletion cascades.
- **Static evidence:** initial `npm run verify` revealed React purity lint failure from `Date.now()` inside Today render. The clock check was moved to server schedule mapping; subsequent `git diff --check` and full `npm run verify` passed for the exact 12-path implementation/test scope.
- **Local runtime evidence:** clean local migration + synthetic-seed replay passed using `20260923055301_ph05_schedule_reminders.sql`; database lint at error level passed; focused `ph05_schedule_reminders.test.sql` passed 27 assertions; complete repository pgTAP regression passed; complete authenticated Playwright suite passed all 17 tests, including reminder versioning, due display and export v9.
- **Environment/source integrity:** runtime harness restored `.env.local` exactly, stopped only script-started local Supabase, and verified unchanged source hashes across the same 12-path scope. Local development database was explicitly reset for synthetic verification. No production database or deployment is proven.
- **Locally verified:** in-app reminder settings, immutable history, effective-occurrence projection and app-view due indication, owner isolation and export v9.
- **Still Unproven:** third-slice commit/push/PR, exact-head remote CI and merge; push/email/SMS or other background delivery and any notification guarantees; progression `REQ-021`-`REQ-023`; final `VAL-028`/PH-05 closure; production/legal/privacy/recovery/support-matrix gates.

### `BR-20260924-01`
- **Mode/scope:** PH-05 first conservative progression **review-only** prerequisite; partial `REQ-021`–`REQ-023`, `AC-009`, `AC-010` and `VAL-028`, with scoped `VAL-024`, `VAL-032`, `VAL-033` and `VAL-036` evidence. No schedule-application or true reversal claim.
- **Baseline/publication:** local branch `agent/ph05-progression-review` started at `b50d92ca13a4bca2b19a9a0e2209a7b636ddf28c`, which GitHub identifies as PR #30's reminder merge into `main` (reminder head `036f1ec5afa6aa9159cc0010b92a260937242a13`; CI #55 run `35833474112` successful). No commit/push/PR is recorded for this progression slice.
- **Implementation:** migration `20260923085723_ph05_progression_reviews.sql` adds owner-private immutable review snapshots, append-only proposal/dismissal events, explicit bounded pause/weekday-reduction previews pinned to exact current plan/schedule versions, stale-write/foreign-owner rejection, pending-review idempotency, plan-detail review/dismiss controls and export v10 with full event history. Self-reported discomfort maps to a pause preview without diagnosis or application.
- **Focused runtime:** user-returned local test log verifies repaired focused progression pgTAP 25/25 without reset and the complete authenticated Chromium suite 18/18, including proposal preview, export v10, immutable dismissal and foreign-plan denial. The repair scoped account-deletion assertions to the two progression fixture reviews and their events rather than counting unrelated preserved-volume records.
- **Other local verification:** user-returned V3 gate log verifies `git diff --check`, lint (zero errors, two existing unused-parameter warnings), typecheck, unit/component 21/21 and production build. Its first full preserved-volume pgTAP run exposed three global-count assertions in the earlier schedule-exception/reminder test files; this was test-isolation evidence, not proof of an FK defect. The next user-returned run reports minimal fixture-ID-based corrections to those two tests, the same original 12 implementation paths unmodified, full pgTAP 366/366 in 17 test files and database lint with no schema errors. No database reset/replay occurred in those two runs; the script stopped only the local Supabase stack it started.
- **Source/evidence boundary:** prior script reports 14 changed/untracked implementation/test paths after the two legacy-test corrections, clean Git index and branch/HEAD checks; those reports are not independent Git remote publication. The attached current local source archive SHA-256 is `111EEE38E00DDADAD3D0E3861EFF1E74F4ACE82A0AAEE3AE130BAE7DE5945A43`. No generated governance changes were included in that evidence archive.
- **Locally verified scope:** review-only bounded explicit-feedback proposal, non-applied disclosure, dismissal history, source-version concurrency checks, owner isolation, primary-datastore fixture deletion and export-v10 progression history. Previously verified schedule behaviour remains covered by the full preserved-volume pgTAP regression.
- **Unproven/outstanding:** clean migration plus synthetic-seed replay with the new progression migration; progression slice exact-head GitHub CI/PR/merge; exercise/routine-level progression; applying/reversing an accepted proposal; missed-session handling; background reminder delivery; full `REQ-021`–`REQ-023`, `AC-009`/`AC-010`, `VAL-028` and PH-05 closure; production/legal/privacy/recovery/support-matrix gates.
